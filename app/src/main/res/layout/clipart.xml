<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/image_border"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        android:src="@drawable/border" />

    <TextView
        android:id="@+id/clipart"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        android:background="@drawable/bg_text_view_edit"
        android:gravity="center"
        android:includeFontPadding="false"
        android:padding="5dp" />

    <ImageButton
        android:id="@+id/image_text_remove"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:adjustViewBounds="true"
        android:background="@drawable/bg_item_text"
        android:padding="1dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_close"
        android:tint="@color/color_25" />

    <ImageButton
        android:id="@+id/image_text_rotate"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:adjustViewBounds="true"
        android:background="@drawable/bg_item_text"
        android:padding="3dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_text_rotate" />

    <ImageButton
        android:id="@+id/image_text_zoom"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:adjustViewBounds="true"
        android:background="@drawable/bg_item_text"
        android:padding="3dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_zoom" />

    <ImageButton
        android:id="@+id/image_text_balance"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:adjustViewBounds="true"
        android:background="@drawable/bg_item_text"
        android:padding="2dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_balance" />

</RelativeLayout>